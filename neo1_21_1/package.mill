package build.neo1_21_1

import mill.*
import build.ModDev.MDG

object `package` extends MDG {
  object platform extends MDG.Platform

  object core extends Core {
    def jvmVersion = "21"
    def enableBsp = build.bspEnabled("neo1_21_1")
  }

  // no remap needed
  override def coreRemappedJar: T[PathRef] = core.assembly
}
